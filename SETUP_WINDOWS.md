# Vasudha — Windows setup

Pure-Python backend (no Visual Studio / Rust required).

```powershell
cd backend
python -m pip install -r requirements.txt
python run.py
```

```powershell
cd frontend
npm install
npm run dev
```

- http://127.0.0.1:8000/health  
- http://localhost:5173  

Login: facility@demo.com / demo123
